(function(window) {
    var app = window.ArcheryTimer || (window.ArcheryTimer = {});

    app.state = app.state || {};
    app.dom = app.dom || {};
    app.core = app.core || {};
    app.ui = app.ui || {};
    app.bridge = app.bridge || {};

    window.ArcheryTimer = app;
})(window);
